version: "3.9"
services:
  lime-app:
    image: acspri/limesurvey:6.3.6
    volumes:
      - ./upload:/var/www/html/upload
      - ./plugins:/var/www/html/plugins
      - ./config:/var/www/html/application/config
    ports:
      - "127.0.0.1:8087:80"
    depends_on:
      - lime-db
    environment:
      - LIMESURVEY_DB_HOST=lime-db
    env_file:
      - ./.env
    container_name: lime-app
  lime-db:
    image: mariadb:10.6
    volumes:
      - db-data:/var/lib/mysql
    env_file:
      - ./.env
    ports:
      - "127.0.0.1:3302:3306"
    container_name: lime-db

volumes:
  db-data:
