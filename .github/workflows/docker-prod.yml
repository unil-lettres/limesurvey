name: docker-prod
on:
  push:
    branches:
      - 'main'
    tags:
      - '*'
permissions: read-all

jobs:
  evaluate-jobs:
    name: Determine jobs execution
    runs-on: ubuntu-latest
    outputs:
      run-merge-build: ${{ steps.determine.outputs.run-merge-build }}
      limesurvey-tag: ${{ steps.version.outputs.tag }}

    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          ref: main

      - name: Extract LimeSurvey tag from Dockerfile
        id: version
        run: |
          LIMESURVEY_TAG=$(grep -oP 'FROM acspri/limesurvey:\K[0-9]+\.[0-9]+\.[0-9]+' Dockerfile)
          echo "tag=$LIMESURVEY_TAG" >> $GITHUB_OUTPUT
          echo "Extracted LimeSurvey tag: $LIMESURVEY_TAG"

      - name: Determine jobs execution
        id: determine
        uses: unil-lettres/actions/job-execution@main
        with:
          image_repository: ${{ vars.DOCKERHUB_REPOSITORY }}
          dockerhub_username: ${{ secrets.DOCKERHUB_USERNAME }}
          dockerhub_token: ${{ secrets.DOCKERHUB_TOKEN }}
          version: ${{ steps.version.outputs.tag }}

  build:
    name: Build
    if: needs.evaluate-jobs.outputs.run-merge-build == 'true'
    runs-on: ${{ matrix.os }}
    needs: [evaluate-jobs]
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            arch: linux/amd64
          - os: ubuntu-26.04-arm
            arch: linux/arm64

    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          ref: main

      - name: Build image
        uses: unil-lettres/actions/docker-build@main
        with:
          dockerfile: ./Dockerfile
          platform: ${{ matrix.arch }}
          image_repository: ${{ vars.DOCKERHUB_REPOSITORY }}
          name_prefix: app
          dockerhub_username: ${{ secrets.DOCKERHUB_USERNAME }}
          dockerhub_token: ${{ secrets.DOCKERHUB_TOKEN }}

  merge:
    name: Merge
    if: needs.evaluate-jobs.outputs.run-merge-build == 'true'
    runs-on: ubuntu-latest
    needs: [evaluate-jobs, build]

    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          ref: main

      - name: Download digests
        uses: actions/download-artifact@v8
        with:
          path: ${{ runner.temp }}/digests
          pattern: digests-*
          merge-multiple: true

      - name: Merge digests
        uses: unil-lettres/actions/docker-merge@main
        with:
          image_repository: ${{ vars.DOCKERHUB_REPOSITORY }}
          tag: ${{ github.ref_type == 'tag' && needs.evaluate-jobs.outputs.limesurvey-tag || 'latest' }}
          dockerhub_username: ${{ secrets.DOCKERHUB_USERNAME }}
          dockerhub_token: ${{ secrets.DOCKERHUB_TOKEN }}

  update-description:
    name: Update Description
    runs-on: ubuntu-latest
    needs: [merge]
    if: success()
    continue-on-error: false

    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          ref: main

      - name: Update description
        uses: peter-evans/dockerhub-description@v5
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
          repository: ${{ vars.DOCKERHUB_REPOSITORY }}
